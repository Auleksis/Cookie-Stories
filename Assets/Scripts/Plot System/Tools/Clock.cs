using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Clock : MonoBehaviour
{
    /*public void StartWaiting(ActionWait action)
    {
        StartCoroutine(Wait(action));
    }

    private IEnumerator Wait(ActionWait action)
    {
        action.unit.SetCanMove(false);
        yield return new WaitForSeconds(action.waitForSeconds);
        action.unit.SetCanMove(true);
        action.isDone = true;
    }*/
}
